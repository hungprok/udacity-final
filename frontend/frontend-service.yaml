apiVersion: v1
kind: Service
metadata:
  name: todo-frontend
  labels:
    app: todo-frontend
spec:
  ports:
    - port: 8100
      targetPort: 80
      protocol: TCP
  selector:
    app: todo-frontend
